version: 1
backend:
  phases:
    build:
      commands:
        - echo "Installation des dépendances backend..."
        - npm install
frontend:
  phases:
    preBuild:
      commands:
        - echo "Pas de frontend, serveur Node.js pur"
    build:
      commands:
        - echo "Démarrage du serveur Express"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*